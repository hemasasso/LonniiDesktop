<Window x:Class="Lonnii.Client.Views.Dialogs.CustomDateRangeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Période personnalisée"
        Height="280" Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource Surface}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Période personnalisée" Style="{StaticResource PageTitle}" />
            <TextBlock Style="{StaticResource PageSubtitle}" Text="Choisissez la période à afficher." />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Du" Style="{StaticResource FieldLabel}" />
            <DatePicker x:Name="DateDebutPicker" Margin="0,4,0,0" Padding="8,6" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Au" Style="{StaticResource FieldLabel}" />
            <DatePicker x:Name="DateFinPicker" Margin="0,4,0,0" Padding="8,6" />
        </StackPanel>

        <Border x:Name="ErrorPanel" Grid.Row="3" Visibility="Collapsed"
                VerticalAlignment="Top" Margin="0,10,0,0"
                Background="{DynamicResource DangerLight}"
                BorderBrush="{DynamicResource Danger}" BorderThickness="1"
                CornerRadius="3" Padding="10,8">
            <TextBlock x:Name="ErrorText" Foreground="{DynamicResource Danger}"
                       TextWrapping="Wrap" FontSize="12" />
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Annuler" Style="{StaticResource SecondaryButton}"
                    Margin="0,0,8,0" Click="Cancel_Click" />
            <Button Content="Appliquer" IsDefault="True"
                    Style="{StaticResource PrimaryButton}" Click="Confirm_Click" />
        </StackPanel>
    </Grid>
</Window>
