using Lonnii.Setup;

// Internal tool: issues the encrypted credentials file for a new customer. See
// SetupCommand for why this is a separate program rather than a verb on the API.
return SetupCommand.Run(args);
